{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "verifier-mp",
  "owner": {
    "name": "kazukinagata"
  },
  "metadata": {
    "description": "Local marketplace for re-verifying Claude Code plugin findings"
  },
  "plugins": [
    {
      "name": "verifier",
      "source": "./verifier",
      "description": "Probe plugin for re-verifying findings recorded in research.md"
    },
    {
      "name": "verifier-violator",
      "source": "./verifier-violator",
      "description": "Companion plugin used to trigger validator errors (probes 02 and 20)"
    },
    {
      "name": "verifier-violator-userpromptexpansion",
      "source": "./verifier-violator-userpromptexpansion",
      "description": "Isolated UserPromptExpansion violator for probe 20"
    },
    {
      "name": "verifier-violator-uppercase-name",
      "source": "./verifier-violator-uppercase-name",
      "description": "Isolated kebab-case violator for probe 20"
    },
    {
      "name": "cowork-env-probe",
      "source": "./cowork-env-probe",
      "description": "Minimal env-propagation probe (plugin-level / frontmatter / body) for CLI vs Cowork comparison"
    },
    {
      "name": "cowork-block-probe",
      "source": "./cowork-block-probe",
      "description": "Skill-to-skill PreToolUse:Skill block test for CLI vs Cowork"
    },
    {
      "name": "cowork-expansion-probe",
      "source": "./cowork-expansion-probe",
      "description": "top-level vs bash -c shell expansion test for CLI vs Cowork"
    },
    {
      "name": "cowork-presub-probe",
      "source": "./cowork-presub-probe",
      "description": "CLAUDE_PLUGIN_ROOT pre-substitution test (top-level vs bash -c) for CLI vs Cowork"
    },
    {
      "name": "fm-presub-probe",
      "source": "./fm-presub-probe",
      "description": "Frontmatter hook pre-sub vs shell-exp test (CLI-only)"
    },
    {
      "name": "cowork-fs-probe",
      "source": "./cowork-fs-probe",
      "description": "hook(host) vs Bash tool(VM) filesystem isolation test"
    },
    {
      "name": "cowork-userconfig-probe",
      "source": "./cowork-userconfig-probe",
      "description": "userConfig sensitive x required combination test"
    },
    {
      "name": "cowork-envfile-probe",
      "source": "./cowork-envfile-probe",
      "description": "CLAUDE_ENV_FILE hook->Bash-tool env propagation test"
    },
    {
      "name": "cowork-surface-probe",
      "source": "./cowork-surface-probe",
      "description": "Isolate which SessionStart hook command form fails to surface on Cowork"
    },
    {
      "name": "cowork-blockmethods-probe",
      "source": "./cowork-blockmethods-probe",
      "description": "PreToolUse block-method matrix (decision/permissionDecision/exit2) CLI vs Cowork"
    },
    {
      "name": "cowork-mp-script-probe",
      "source": "./cowork-mp-script-probe",
      "description": "Tests ${CLAUDE_PLUGIN_ROOT}/hooks/script.sh launch in plugin-level SessionStart hook. Designed to be installed via the Cowork GUI marketplace flow (not zip upload) and compared against zip-upload baseline."
    },
    {
      "name": "cowork-mp-disambig-probe",
      "source": "./cowork-mp-disambig-probe",
      "description": "Disambiguator probe. Adds double-quote and $HOME observation points to settle whether Cowork hook command runtime is (a) shell-bypassed launcher or (b) bash -c with $-escape + path-prefix pre-sub."
    },
    {
      "name": "cowork-fm-bashblock-probe",
      "source": "./cowork-fm-bashblock-probe",
      "description": "Proves whether a skill frontmatter PreToolUse:Bash hook fires on Cowork by emitting only a block decision (no echo). Mac re-verification of frontmatter hook behavior."
    },
    {
      "name": "cowork-bodypath-probe",
      "source": "./cowork-bodypath-probe",
      "description": "Tests skill-body ${CLAUDE_PLUGIN_ROOT} substitution value on Mac Cowork, whether the VM Bash tool can use it, and whether the Read tool can read a bundled file at that host path (article section 3)."
    },
    {
      "name": "cowork-exec-form-probe",
      "source": "./cowork-exec-form-probe",
      "description": "Tests hook command exec form (args present) on Cowork: whether ${CLAUDE_PLUGIN_ROOT} resolves to a real path via launcher text pre-substitution vs env-var export, where shell form (§2.1/§2.2) failed both."
    },
    {
      "name": "cowork-pretooluse-shell-probe",
      "source": "./cowork-pretooluse-shell-probe",
      "description": "Tests whether Cowork runs plugin-level PreToolUse hook commands through cmd.exe (like SessionStart) or a POSIX shell, and whether the inline tool_input=$(cat)|grep&&echo-block pattern actually blocks the bash tool."
    },
    {
      "name": "cowork-upe-probe",
      "source": "./cowork-upe-probe",
      "description": "Tests whether a plugin-level UserPromptExpansion hook fires on Cowork (slash-invoked), with SessionStart/UserPromptSubmit controls. Windows: expected validator reject (§2.3); macOS: may install, letting firing be observed for the first time."
    },
    {
      "name": "cowork-data-persist-probe",
      "source": "./cowork-data-persist-probe",
      "description": "Tests whether a value persisted to $CLAUDE_PLUGIN_DATA from a plugin-level SessionStart hook survives across Cowork chat sessions. Write+read-back runs in the host hook (persist.sh) because the VM body has DATA unset; surfaces accumulated per-session markers so cross-session persistence is read from SessionStart context."
    }
  ]
}
